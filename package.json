{
  "name": "horn",
  "private": true,
  "scripts": {
    "bootstrop": "lerna bootstrop --hoist",
    "dev": "lerna run dev --scope @daysnap/horn",
    "clean": "lerna clean",
    "husky": "husky install && echo 'export PATH=\"/usr/local/bin/:$PATH\"' >> ~/.huskyrc"
  },
  "devDependencies": {
    "@commitlint/cli": "^16.2.1",
    "@commitlint/config-conventional": "^16.2.1",
    "commitizen": "^4.2.4",
    "husky": "^7.0.4"
  },
  "workspaces": [
    "packages/*"
  ],
  "publishConfig": {
    "access": "public"
  }
}
