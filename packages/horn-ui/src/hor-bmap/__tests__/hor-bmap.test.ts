import { describe, expect, it } from 'vitest'
import { mount } from '@vue/test-utils'
import { HorBmap } from '../index'

describe('HorBmap', () => {
  it('create', () => {
    const wrapper = mount(HorBmap, {
      props: { },
    })
  })
})
